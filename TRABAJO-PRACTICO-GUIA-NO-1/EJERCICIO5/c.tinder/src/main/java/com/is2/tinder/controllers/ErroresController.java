package com.is2.tinder.controllers;

import org.springframework.stereotype.Controller;

@Controller
public class ErroresController {
    // Intentionally left without @RequestMapping to avoid conflicting with Spring Boot's default /error handling.
}